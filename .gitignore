# Credentials — never commit (PS2 rubric caps the score for a committed key)
.env
.env.*
!.env.example
*.key

# Local scratch
generated_temp/

# PS2 backend — virtualenv and raw upstream cache (derived outputs ARE committed)
PS2/backend/.venv/
PS2/backend/data/cache/
__pycache__/
*.pyc
